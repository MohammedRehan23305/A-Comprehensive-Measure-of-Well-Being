Import the required libraries to build the web application. Flask is used as the web framework to create and manage the application routes, while Pickle is used to load the pre-trained machine learning model stored in a .pkl file. NumPy is imported to handle numerical input data required for prediction.
