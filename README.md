Generate HDI Predictions: Run the model on the test set and print the predicted HDI values. print(y_pred)

Calculate R-Squared Value: R-squared (R²) measures the proportion of variance in the dependent variable explained by the independent variables. Test with Fewer Values: Test the model with a reduced input set to validate predictions for individual data points. Inspect y_test Values: Print the actual y_test values (ground truth HDI scores) as an array for comparison. Inspect y_pred Values: Print the predicted y_pred values. These should be very close to the y_test values, confirming the model's accuracy.
